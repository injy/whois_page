node_modules/
.wrangler/
dist/
dist-worker/

# Reference PHP project (not part of this repo)
20260818/

# Static build output (served from dist/) and the root index.html fallback.
# NOTE: cloud-functions/ and middleware.js MUST stay TRACKED — EdgeOne scans the
# committed repo to detect server functions / edge middleware, so they cannot be
# gitignored even though build:pages regenerates them at deploy time.
index.html

*.log
