{
  "name": "whois-cf",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "wrangler deploy --dry-run --outdir=dist-worker",
    "build:pages": "node scripts/build-edgeone.mjs",
    "dev:pages": "edgeone pages dev"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240512.0",
    "esbuild": "^0.17.19",
    "typescript": "^5.4.5",
    "wrangler": "^3.57.0"
  }
}
